---
# bjw-s app-template loader. Resource names derive from the release name or an
# explicit global.nameOverride / global.fullnameOverride set by the consuming
# app (see README). No hardcoded nameOverride here — pinning it caused every
# release to be named "cardiac-uhn" and collide under the umbrella pattern.
{{- include "bjw-s.common.loader.all" . }}
